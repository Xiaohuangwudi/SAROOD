network:
  name: resnet18_64x64
  num_classes: '@{dataset.num_classes}'
  pretrained: False          # set 'True' to load pretrained model
  checkpoint: results/mstar_resnet18_64x64_base_e100_lr0.1_default/s0/best_epoch91_acc0.9737.ckpt         # ignore if pretrained is false
  num_gpus: '@{num_gpus}'
